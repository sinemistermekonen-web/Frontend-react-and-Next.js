function Header() {
    return (
        <header className="header">
            <h1> Addis cafe</h1>
            <nav>
             <a href="#home">Home</a>
             <a href="#menu">menu</a>
             <a href="#contact">contact</a>
            </nav>
        </header>
    );
}

export default Header;